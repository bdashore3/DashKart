This game requires processing.py version 3. Download it from here: https://processing.org/download/
Install the python module and the sound library

To play the game on a mac, you need to disable keyRepeat

open terminal and paste this command:

defaults write -g ApplePressAndHoldEnabled -bool false

To re-enable keyRepeat, change false to true in the previous command